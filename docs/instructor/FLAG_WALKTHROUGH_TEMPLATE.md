# Challenge: <Name>

| Field | Value |
|---|---|
| Flag | `FLAG{...}` |
| OWASP Category | Axx |
| Difficulty | Easy/Medium/Hard |
| Route/Page |  |
| Required Role/Account |  |
| Primary Weakness |  |
| Related CWE if known |  |
| Expected Evidence Sources | Apache, `app_events.jsonl`, fake audit log, database, Wazuh |
| Detection Opportunity |  |
| Remediation Theme |  |

## 1. Objective

State what the learner must demonstrate and what evidence makes the solve valid.

## 2. What the Player Sees

Describe the normal workflow, visible controls, hints, and expected browser behavior without assuming source-code access.

## 3. What Is Actually Happening

Explain the root cause, relevant trust boundary, server-side decision, and where the flag is stored or rendered.

## 4. Step-by-Step Lab Walkthrough

Keep all steps local to the lab. Use only minimal, challenge-specific examples; do not provide real-target instructions or payload catalogs.

## 5. Evidence Trail

Document all applicable sources and explicitly identify gaps:

- HTTP request/response clues;
- browser-visible clues;
- database-visible data if relevant;
- Apache log visibility;
- `/var/log/vulnforge/app_events.jsonl` visibility;
- fake in-app audit log visibility; and
- Wazuh alert visibility if configured.

## 6. Wazuh Notes

- **Likely `event_type`:**
- **Likely route:**
- **Likely rule name, ID, or group if present:**
- **Sample dashboard query:**
- **What the alert proves:**
- **What the alert does not prove:**

## 7. Noise and Detection-Awareness Notes

Allowed discussion:

- Explain which legitimate logs are generated by the lab action.
- Compare noisy beginner behavior with careful, low-volume validation.
- Explain why low-volume testing may be harder to distinguish from normal use.
- Explain which sources defenders should correlate.

Prohibited discussion:

- Do not provide instructions to bypass Wazuh or SIEM detection.
- Do not provide evasion payloads.
- Do not provide log-tampering instructions.
- Do not provide advice for avoiding attribution on real systems.

## 8. Common Mistakes

List validation errors, incorrect assumptions, and evidence gaps that commonly prevent a defensible solve.

## 9. Remediation

Describe secure design and implementation changes without modifying the intentionally vulnerable lab.

## 10. Detection Engineering Improvement

Propose useful fields, rules, thresholds, correlations, and validation tests. Separate prevention from detection.

## 11. Analyst Takeaway

Summarize the most important evidence and confidence lesson in one or two paragraphs.
